//Import file with folder structure and then filename.bn
import "test/utils.bn" 

//main fun with using the imported functions
fun main() {
    var num = power(2, 3)    // Uses imported power function
    print("2^3 = " + toString(num))
}
